concurrname: Smoke tests minimal

on:
  push:
    branches: [ "main", "release-v1.3" ]
  pull_request:
    branches: [ "main" ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo
        run: echo "smoke minimal OK"ency: 
